# NODO — дорожная карта

Источник находок: `AUDIT-2026-09.md`. Отмечай `[x]` сделанное и пиши дату/коммит в `STATE.md`.
Рамки владельца: пока только DeepSeek и Codex; безопасность и Claude — отложены (см. `DECISIONS.md`).

## Этап 0 — быстрые исправления ✅ (2026-09-25)
- [x] Восстановлен `services/lifecycle-gate.cjs` (плагин DSH, сборка, тесты quit/services падали)
- [x] Остановленная задача больше не блокирует обновление: `Paused` не мешает preflight; отмена
      пользователем → `Cancelled`; `cancel()` ничего не делает с завершённой задачей
- [x] Гонка таймаута Codex: дедлайн ставится до `turn/start`, ранний `turn/completed` его снимает
- [x] Проверки «здорового» старта после обновления (`main.cjs` и `scripts/update-preflight.cjs health`,
      которую вызывает Rescue) больше не требуют Codex — только предупреждение
- [x] Codex: вывод в UTF-8 (кириллица не бьётся), kill при таймауте `initialize`, автоперезапуск с backoff
- [x] Smart Context: ранжирование по свежести (`recencyOf` получал не тот аргумент)
- [x] `project-runtime`: опечатка `sourcesCache` → живой статус контекста снова работает
- [x] Супервизор: русские вопросы распознаются (`\b` не работал с кириллицей); `?` внутри URL — не вопрос
- [x] Иконка Dock (`icon_512x512@2x.png`) и репозиторий обновлений `stgavs77-ctrl/NODO`
- [x] Экран загрузки показывает ошибку старта вместо вечного «Starting NODO…»
- [x] `npm test`, `npm run inventory[:check]`, `CLAUDE.md`/`AGENTS.md`, память проекта в `docs/project/`
- [x] На реальном Mac: `bootstrap`, `build:dev`, запуск NODO DEV - работают (2026-09-25)
- [x] Живая задача DeepSeek в DEV 1.4.39 (через DEV-прокси на интерфейс VPN), 2026-09-25
- [ ] Живая задача Codex (владелец пока не вошёл в ChatGPT)

## Объединение с рабочей версией 1.4.39
- [x] Сравнение и план (`MERGE-PLAN.md`), решение владельца - вариант A
- [x] Исправления ветки перенесены в локальную копию `~/Projects/NODO-private` (`merge/audit-fixes`)
- [x] Рабочая установка обновлена до 1.4.41 (25.09; 1.4.40 откатывалась - нет client-card в бандле)
- [ ] Влить `merge/audit-fixes` в `~/NODO` (решение владельца)

## Этап 1 — стабильность (следующий)
- [x] Отрицательная «экономия» в шапке: устаревшая точка баланса (исправлено в `NODO-private`); остаток - низкий кэш-хит
- [x] Кэш-хит 89% против 98.6%: причина - смена уровня рассуждения на каждом шаге; роутер выключен
- [ ] Холодный первый запрос короткой сессии (~90k некэшированных токенов): разобрать, что в него входит
- [x] cost-meter: каждая запись по тарифу своего момента (`costRows`), записи Codex со временем,
      блок «сегодня» в `project()` считался от текущего момента вместо начала дня; тесты не зависят от часов
- [x] Атомарная запись `io.save`: уникальный tmp + fsync; debounce записи `tasks.json` при стриминге
- [x] Чистка старых задач в `tasks.json` (300 последних завершённых)
- [x] Битый JSON не роняет запуск: откладывается в `.corrupt-<время>` (`io.read` с fallback); битый чекпоинт не скрывает остальные
- [x] Задачи DeepSeek: дедлайн (по умолчанию 120 мин, максимум 240), до 5 повторов с backoff при ошибке DSH
- [ ] Задачи DeepSeek: инкрементальный `inspect` (сейчас каждый опрос читает всю историю)
- [x] Супервизор: жёсткая отмена хода при зависшем инструменте (после 5 grace-периодов), `.catch` на async cancel
- [ ] Супервизор: прогресс по реальным изменениям файлов (обход `touch && curl`, `supervisor.cjs:746/777`),
      повторная проверка фазы в async-верификаторе (`:1071`)
- [x] `lifecycle-control.pause()` сбрасывает `paused` при ошибке
- [ ] Убрать синхронный `execFileSync git` / обход ФС из главного потока (`project-runtime.cjs:197`)
- [ ] Чистка `data/tmp/nodo-*` при выходе; ограничение и чистка чекпоинтов
- [ ] Компакция: порог от окна модели, `retainTokens` < порога, исправить комментарий (`profile.patch.yml`)
- [ ] Контекст: фильтр документов по релевантности; пустой шаг `past-sessions` в Economy; проверить
      повторную вставку reference-блока на каждом шаге (`project-runtime.cjs:120-135`)

## Этап 2 — Codex как полноценный агент (DeepSeek + Codex)
- [ ] Codex работает в папке проекта пользователя, `sandbox: workspace-write`, родные shell/patch
- [ ] Обработчик `tasks.approve` (сейчас запрос разрешения можно только отклонить)
- [ ] Вход по API-ключу OpenAI как альтернатива ChatGPT
- [ ] `codex-config.toml` обновляется у существующих установок (сейчас копируется только один раз)
- [ ] Роутер: по сильным сторонам, с причиной решения в UI; учитывать `balance`/`large`
- [ ] Общий интерфейс runtime (`start/status/validate/run/cancel/usage`) — подготовка к третьему провайдеру
- [ ] Цены по модели (не только DeepSeek), реальный счёт токенов вместо `chars/4`

## Этап 3 — удобство и процесс
- [ ] CI (GitHub Actions): юнит-тесты на Linux, полный набор на macOS
- [ ] ESLint + `tsc --checkJs` для `lib/`; `engines` в package.json
- [ ] `extension/client.js` → исходники в `src/` + esbuild
- [ ] Все строки UI через `t()`; статус задачи по коду, а не по слову «готово»
- [ ] Диалоги вместо `confirm()`, понятные тексты ошибок, доступность (aria, клавиатура)
- [ ] Патчи DSH по якорям вместо байтовых смещений; уход с `0.1.5-rc.1`
- [ ] Экспорт публичной версии проверяет, что все `require()` разрешаются

## Отложено по решению владельца
**Безопасность:** CDP-порт и тестовый HTTP-сервер только в DEV; Codex `keyring`; токен DSH не в логе;
ключ DeepSeek не в env инструментов; блокировка loopback/LAN во встроенном браузере; GitHub-токен при
редиректе; Remote/relay (доверие к клиенту, подтверждение на Mac); Telegram (stdin, подтверждение);
вспомогательная модель только HTTPS + opt-in; ротация ключей подписи; нотаризация.
**Claude:** третий runtime через Claude Agent SDK / `claude -p --output-format stream-json`.
