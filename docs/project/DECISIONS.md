# Решения и ограничения

Записывай сюда решения владельца и архитектурные выборы: дата, решение, причина.

| Дата | Решение | Причина / контекст |
| --- | --- | --- |
| 2026-09-24 | Провайдеры: только DeepSeek (основной) и Codex. Claude не интегрировать пока. | Решение владельца после аудита |
| 2026-09-24 | Задачи безопасности из аудита отложены. | Решение владельца; список в ROADMAP «Отложено» |
| 2026-09-25 | Остановленная пользователем задача получает статус `Cancelled` (терминальный); `Paused` остаётся за супервизором и не блокирует preflight обновления. | Иначе одна остановленная задача навсегда блокировала обновление |
| 2026-09-25 | Codex не входит в условие «здорового старта» после обновления; статус Codex пишется в `last-known-good.json` как информация. | Падение Codex не должно вызывать откат рабочей сборки с DeepSeek |
| 2026-09-25 | `services/lifecycle-gate.cjs` восстановлен по контракту тестов. Приватные `telegram-bridge-live.mjs` и `sessions-observer.mjs` не выдумываем; их тест пропускается, если файлов нет. | Публичный экспорт потерял `services/`. Если у владельца есть оригинал — сравнить и заменить |
| 2026-09-25 | Память между сессиями хранится в `docs/project/` (STATE, ROADMAP, DECISIONS, AUDIT) + `CLAUDE.md`/`AGENTS.md`. | Облачные сессии эфемерны; оба агента (Claude, Codex) читают эти файлы |
| 2026-09-25 | Объединение: база - рабочая версия владельца 1.4.39 (`~/NODO`), вариант A из `MERGE-PLAN.md`. Исправления ветки переносятся в локальную приватную копию, `~/NODO` не трогаем. Приватное дерево в публичный GitHub не пушится (см. `PUBLISH-EXCLUDES.txt` владельца). | Решение владельца; публичный репозиторий отстаёт на 264 коммита |
| 2026-09-25 | Роутер усилия (`effort-router`) выключен по умолчанию, включение флагом `effortRouter` в `economy-flags.json`. | Замер 17-25.09: смена уровня посреди хода сбрасывает кэш DeepSeek, потери $11.68 против экономии $0.59 |
| 2026-09-25 | Сжатие харнесса - мягкий порог: после 400k токенов, дословный хвост 150k. | Старое (≈150k, хвост 50k) теряло правила клиента; без сжатия контекст доходил до 754k. Цена почти ноль |
| 2026-09-25 | Новый дизайн: графит + один акцент бирюза #4FB7A8, логотип N из четырёх узлов, «живой узел» как индикатор агента. Заменяет решение «бренд строго белый, без своей палитры». | Владелец утвердил эскиз и попросил внедрить |
