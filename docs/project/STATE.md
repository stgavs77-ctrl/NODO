# Состояние проекта и журнал сессий

## Сейчас
- Версия: NODO 1.4.0. Ветка работы: `claude/nodo-hsrness-audit-gdh38c`.
- Этап 0 выполнен (кроме проверки на реальном Mac). Следующий — **этап 1, стабильность**.
- `npm test` на Linux: новых падений нет. Ожидаемые падения из-за окружения перечислены в `CLAUDE.md`.
  Три теста cost-meter падают в пиковые часы DeepSeek (это баг, первый пункт этапа 1).

## Следующий шаг
1. Владелец: на Mac выполнить `npm run bootstrap && npm run build:dev`, запустить NODO DEV,
   проверить задачи DeepSeek и Codex. Если есть оригинал `services/lifecycle-gate.cjs` из приватного
   дерева, сравнить его с восстановленным.
2. Агент: этап 1 из `ROADMAP.md`. Начать с cost-meter (тариф по времени записи) и атомарной записи `io.save`.

## Открытые вопросы к владельцу
- Где лежит приватное дерево (`services/telegram-bridge-live.mjs`, `sessions-observer.mjs`,
  `scripts/export-public.cjs`, `start-current-nodo.command`)? Нужно ли переносить его в репозиторий?
- Правильно ли `stgavs77-ctrl/NODO` выбран источником обновлений? В публичном дереве была заглушка
  `example-org/NODO`.

## Журнал
### 2026-09-24/25 — аудит и этап 0 (Claude Code)
- Проведён полный аудит в четыре параллельных прохода. Результат лежит в `AUDIT-2026-09.md`.
- Этап 0: восстановлен `services/lifecycle-gate.cjs`. Исправлены:
  - статусы задач, preflight и гонка таймаута Codex;
  - условие «здорового» старта;
  - в Codex: UTF-8, kill и перезапуск;
  - `recencyOf`, опечатка `sourcesCache`, распознавание вопросов на русском и `?` в URL;
  - иконка, репозиторий обновлений, ошибка на экране загрузки.
- Добавлены регрессионные тесты для:
  - preflight с `Paused`;
  - гонки Codex и отмены завершённой задачи;
  - классификатора сообщений.
- Два теста, которым нужны приватные файлы, получили явный skip с причиной.
- Добавлены `npm test`, `npm run inventory[:check]` (`scripts/source-inventory.cjs`), `CLAUDE.md`,
  `AGENTS.md` и `docs/project/*`.
