# Состояние проекта и журнал сессий

## Сейчас
- Публичное дерево: NODO 1.4.0. Опубликованные релизы уже дошли до **1.4.32** (последний от 16.09.2026):
  их собирали из приватного дерева владельца. Публичный репозиторий отстаёт от реально установленной версии.
- Ветка работы: `claude/nodo-hsrness-audit-gdh38c`.
- Этап 0 выполнен. Этап 1 (стабильность) в работе, см. `ROADMAP.md`.
- CI на macOS (GitHub Actions) собирает NODO DEV.app и гоняет все тесты. Результат последнего прогона
  смотри через `actions_list`. Последний прогон (коммит 4d60ef8) полностью зелёный: bootstrap, сборка, тесты.

## Правило работы с владельцем
Владелец не программист. Не проси его запускать команды, собирать проект или смотреть код. Всё, что
проверяется автоматически, проверяй сам: CI на Mac, GitHub MCP. К владельцу обращайся только за
решениями («что делать»), а не за действиями («как сделать»).

## Следующий шаг
**Главное (с 25.09): рабочие исходники - `~/NODO-release` (ветка `release/1.4.41` репозитория `~/NODO`).
Правила выпуска - `~/NODO-release/docs/RELEASE-RULES.md`. `~/Projects/NODO-private` устарела (не удалять,
но не использовать). Этот публичный репозиторий - только журнал и аудит, из него не собирать.**
Дальше по согласованию с владельцем: холодный первый запрос сессии (~90k), чистка tmp/зависания.
0. Объединение A сделано локально: `~/Projects/NODO-private`, ветка `merge/audit-fixes` (снимок 1.4.39 +
   перенесённые исправления). В GitHub НЕ пушится (приватное). Дальнейшую разработку вести там;
   решить с владельцем, когда влить в `~/NODO` и выпустить. Повторить DeepSeek (сеть) и Codex (вход).
1. Этап 1: прогресс супервизора по реальным изменениям файлов; дедлайн и повторы для задач DeepSeek;
   синхронный git/ФС убрать из главного потока; чекпоинты с ограничением.
2. Этап 2: Codex как полноценный агент (workspace-write в папке проекта, кнопка «Разрешить», вход по API-ключу).

## Что выяснено без владельца
- Источник обновлений `stgavs77-ctrl/NODO` подтверждён: там опубликованы релизы до v1.4.32.
- `services/lifecycle-gate.cjs` восстановлен по контракту тестов и проходит их на Mac. Оригинал
  недоступен (приватное дерево).
- В публичном дереве также нет `remote-relay/`, `scripts/telegram-read.py`,
  `start-current-nodo.command`, `vk-factoscope-runner.sh`. Сборка теперь пропускает их с сообщением.
  Rescue (`rescue/*.swift`) по-прежнему упоминает их для watchdog-миграции личной установки.

## Журнал
### 2026-09-24/25 — аудит, этап 0, CI, начало этапа 1 (Claude Code)
- Полный аудит сохранён в `AUDIT-2026-09.md`.
- Этап 0: восстановлен lifecycle-gate. Исправлены:
  - статусы задач и preflight, гонка таймаута Codex;
  - проверки «здорового» старта: Codex больше не обязателен ни в main, ни в update-preflight;
  - Codex: UTF-8, kill и автоперезапуск;
  - `recencyOf`, `sourcesCache`, распознавание вопросов на русском и `?` в URL;
  - иконка, репозиторий обновлений, ошибка на экране загрузки.
- CI на macOS: bootstrap и сборка проходят, по тестам 148 прошли, 5 упали. Причины падений:
  - 3 — нет `remote-relay/`, теперь это явный skip;
  - 1 — приватные скрипты в тесте установщика, теперь skip;
  - 1 — Swift-тест бэкапа сравнивал URL папок со слэшем на конце, тест исправлен.
- Этап 1:
  - cost-meter: каждая запись по тарифу своего времени, записи Codex со временем, исправлено «сегодня»;
  - атомарная запись с fsync, битый JSON уходит в `.corrupt-*`;
  - debounce записи задач, чистка старых задач (хранятся 300 последних завершённых);
  - временная папка удаляется при выходе, Codex не перезапускается при выходе NODO;
  - супервизор жёстко отменяет ход при зависшем инструменте, ошибка cancel ловится;
  - lifecycle pause больше не «залипает».

### 2026-09-25 — первая сессия на Mac владельца (Claude Code)
- `npm run bootstrap` и `npm run build:dev` прошли, NODO DEV стартует (`ready`), профиль/порты изолированы.
  `npm test` локально: 166 тестов, 155 прошли, 0 упали, 11 пропущены.
- DeepSeek в DEV: задача создаётся, но 5 повторов падают с `TRANSPORT` - `api.deepseek.com` с Mac сейчас
  недоступен (DNS 198.18.x.x от VPN Happ, соединение висит). Это сеть, не NODO. Проверку повторить.
- Codex в DEV: app-server готов, аккаунта нет; вход через ChatGPT запущен, владелец решил пока пропустить.
- Найдена рабочая версия владельца `~/NODO` (1.4.39, +264 коммита к публичному 1.4.0, ~115 незакоммиченных
  правок) и оригинальный `services/lifecycle-gate.cjs`. Сравнение и план - `MERGE-PLAN.md`; выбран вариант A.
- Исправления ветки (Codex restart/UTF-8, дедлайны задач, fsync, costRows, `sourcesCache`) в 1.4.39 ещё нет.
- `.gitignore`: симлинк `node_modules` (его создаёт build) больше не виден как неотслеживаемый файл.
- Объединение (вариант A) выполнено в локальной копии `~/Projects/NODO-private` (ветка `merge/audit-fixes`),
  `~/NODO` не тронут. Перенесены: Codex UTF-8/restart, дедлайны и `Cancelled`, лимит и debounce `tasks.json`,
  fsync-запись, цены по времени строки (с таблицей владельца `pricing.cjs`, поправка ×1.3), супервизор,
  lifecycle pause, Codex не обязателен для «здорового старта», чистка tmp при выходе, Swift-фикстура.
  Оставлены версии владельца: `cold-preflight` (очередь возобновления), `lifecycle-gate`, `github.cjs`, экран ошибки.
  Тесты приватного дерева: до переноса 8 падений, после - 4 (все старые: монохром Remote/PWA, cold installer,
  mobile TLS e2e, durable request identity). DEV 1.4.39 из копии собирается и стартует (`ready`).
- Найдено: DEV 1.4.0 не закрывается по SIGTERM (DSH-ребёнок не выходит, принудительного kill нет) - в 1.4.39
  уже исправлено (SIGKILL через 15 с). Задача DeepSeek при ошибке сети честно уходит в `Failed`.
- В DEV-профиле открыт чат с моделью `free-pool`, поле ввода пишет «This model is unavailable».
- **DeepSeek живьём проверен** (DEV 1.4.39 из `NODO-private`): ответ пришёл, статус Done, токены (25 014) и
  баланс считаются, в шапке «потратил $2.62». Штатный выход DEV - 2 с, временная папка удаляется.
- Почему DEV не видел DeepSeek: на Mac сломан маршрут к адресам VPN Happ (198.18.0.0/16 уходит в `en0`
  вместо `utun6`). Рабочий NODO ходит через `utun6` и работает, новые процессы виснут. Happ трогать
  нельзя (решение владельца). Обход только для DEV: локальный CONNECT-прокси, который привязывает исходящие
  соединения к адресу `198.18.0.1` (`net.connect({localAddress})`), и запуск DEV с
  `open --env HTTPS_PROXY=http://127.0.0.1:18899 --env NO_PROXY=127.0.0.1,localhost`. Рабочую версию не трогает.
- Находка: в шапке «сэкономил $-0.95» - отрицательная экономия (есть и в рабочей версии). Разобрать в этапе 1.
- Этап 1, «отрицательная экономия» (в `NODO-private`): монитор баланса `~/.dsh/balance-monitor.log` не достаёт
  DeepSeek с 24.09 10:16 (тот же сломанный маршрут VPN), и `nodo-econ-period` брал вчерашнюю точку баланса
  как начало дня - вчерашние траты шли в сегодня («потратил $2.62»). Исправленная копия скрипта теперь в
  приложении (`scripts/nodo-econ-period.py`, плагин берёт её первой; `~/.dsh` не тронут): точка баланса
  засчитывается только в пределах 30 мин, иначе «потратил» считается по таблице цен и помечается «≈».
  Итог в DEV: «сэкономил ≈$-0.12 · потратил ≈$0.33 · без экономии было бы ≈$0.21».
- Честная находка для владельца: даже без ошибки экономия сегодня отрицательная - попадание в кэш 89%
  против 98.6% у старого профиля, поэтому запрос дороже, хотя контекст меньше на 43%.
- **Почему упал кэш DeepSeek** (разбор сессий рабочего профиля с 24.09, только чтение):
  1) 46 из 48 больших промахов посреди сессии (>20k некэшированных токенов, до 440k за запрос) идут сразу
     после смены `reasoningEffort` в заголовке запроса - её делает `services/effort-router.mjs` на каждый
     шаг (high/low/max). После смены кэш обнуляется до ~0-18k. Это 5.0 млн из 6.5 млн некэшированных
     токенов в непрерывной работе (77%), ≈ +$0.7 в день.
  2) Сессии стали короткие (~20 запросов против ~110 в старом окне 10-11.09), поэтому «холодный» первый
     запрос сессии даёт 30-40% промахов (в среднем ~90k некэшированных токенов на первый запрос).
  Решение (выключить роутер / менять усилие только в начале хода / оставить) - за владельцем.
- **Замер роутера усилия** (рабочий профиль, 17-25.09, 11 704 запроса, цены владельца с пиком):
  экономия на рассуждениях (low вместо high) - $0.59 за 8.4 дня; потеря на кэше из-за смен уровня
  (3 422 запроса после смены) - $11.68. Роутер в минусе примерно в 20 раз (≈ -$1.3 в день). Уровень `low`
  почти не сокращает рассуждения: в среднем 351 токен против 322 у `high`. Скрипт замера:
  `scratchpad/router.py` (не в репозитории).
- Роутер усилия выключен по умолчанию в `NODO-private` (решение владельца 25.09): включается только
  `"effortRouter": true` в `economy-flags.json` профиля. Тест `tests/effort-router-default.test.cjs`.
  Проверено в DEV: многошаговая задача, уровень всё время `high`, кэш после первого шага 96-99%.
- **25.09 12:48 рабочий NODO обновлён до 1.4.40** (по согласию владельца, после перезагрузки Mac) - сборка
  `build:release` из `NODO-private` (коммит `19341ef`), установлена переключателем владельца
  `~/NODO/scripts/nodo-switch.cjs install`: остановка, установка рядом, ready проверен. Откат: `nodo-switch.cjs
  rollback` (1.4.39 лежит в `~/Applications/NODO-versions/1.4.39`). Codex отложен до прямого поручения владельца.
- Помощники для DEV (CDP-скрипт, VPN-прокси) теперь в `~/Projects/nodo-devtools/` (scratchpad стирается перезагрузкой).
- **Установка 1.4.40 провалилась, откат на 1.4.39 выполнен** (25.09 ~12:55, `nodo-switch.cjs rollback`, ready).
  Причина: харнесс падал в цикле с `ERR_MODULE_NOT_FOUND lib/client-card.cjs`. Переключатель увидел `ready`
  у окна раньше, чем упал харнесс, поэтому сам не откатил. Корень: установленную 1.4.39 правили прямо в бандле
  (`client-card.cjs`, `client-swap.cjs`, `bridge-watch-*.py`, `periodic-gate.py` докладывали вручную - их нет
  в `release-allowlist.json`), а в `main.cjs`, `dsh-plugin.mjs`, `telegram-bridge-live.mjs` бандл и `~/NODO`
  разошлись в обе стороны (напр. в бандле защита `compaction`, в дереве - отключённая автокомпакция и
  локальные инструменты). DEV этого не ловит: Telegram-мост в DEV выключен.
  Перед следующей установкой: (1) дополнить allowlist; (2) свести правки бандла 1.4.39 и дерева по каждому файлу;
  (3) проверка сборки - разрешать импорты всех упакованных `.mjs/.cjs`, включая `services/`;
  (4) переключатель должен ждать, пока харнесс проработает ~30 с, а не только `ready`.
- **Готова 1.4.41** (`NODO-private`, ветка `release/1.4.41`, коммит `f857730`; сборка в `~/Projects/NODO-releases/1.4.41/`).
  Базис - файлы установленной 1.4.39 (решение владельца: вести себя как сейчас, правки 19.09 не включать) +
  исправления аудита, экономия без устаревшего баланса, роутер выключен. Сверка бандла с 1.4.39: отличаются
  только наши файлы. Сборка теперь проверяет импорты всех `services/*`; все сервисы, включая Telegram-мост,
  импортируются. Переключатель ждёт 30 с после ready и откатывает, если версия не удержалась (тест).
  DEV 1.4.41: DeepSeek ответил, Done, токены считаются. Тесты: 4 старых падения, новых нет.
  Ставить - новым переключателем из `NODO-private` (у старого в `~/NODO` нет ожидания после ready).
- **25.09 13:06 рабочий NODO обновлён до 1.4.41** (согласие владельца) новым переключателем из `NODO-private`:
  ready + 30 с удержания пройдены; через минуту тот же pid харнесса, ошибок загрузки нет (в `dsh.log` только
  старые записи 1.4.40). Откат: `~/NODO/runtime/node ~/Projects/NODO-private/scripts/nodo-switch.cjs rollback`.
  Дальше: владелец проверяет в работе; Codex - только по прямому поручению.
- Один источник: ветка `release/1.4.41` добавлена в `~/NODO` (fetch, без checkout - 115 правок владельца в `main`
  целы), рабочая папка `~/NODO-release` (git worktree). Сборка оттуда совпала с установленной 1.4.41 байт в байт
  (кроме build-info). В ветку добавлены `docs/RELEASE-RULES.md` и забытый `tests/artifacts/context-economy-reference.json`.
- Рабочий NODO видит DeepSeek через локальный прокси Happ `127.0.0.1:10808` (прописан в `.env` профиля 24.09);
  для DEV можно так же, свой `vpnproxy.cjs` не обязателен.
- **Холодный первый запрос сессии - разобран, чинить не стоит** (25.09). Первый запрос ~36k токенов: в кэш
  идут системный промпт и инструменты (~18-20k), мимо - новое сообщение и затем `~/.dsh/AGENTS.md` (~42k
  символов), который `dsh-agent-instructions` кладёт в inbox «next-step» ПОСЛЕ сообщения, поэтому одинаковые
  инструкции не кэшируются. Цена: 305 сессий за 8.6 дня, медиана 16k некэшированных, всего $1.15 (≈ $0.13 в
  день). Прежняя оценка «~90k на первый запрос» была ошибкой разбиения (в «первые» попадали возобновлённые сессии).
  Выигрыш от переноса инструкций в системный промпт - меньше $0.1 в день ценой патча внутренностей DSH: не делаем.
  Дешёвый рычаг, если захочется: сократить `~/.dsh/AGENTS.md` (84 КБ).
- **1.4.42 собрана и проверена в DEV** (коммит `a5b0094`, сборка `~/Projects/NODO-releases/1.4.42/`), ждёт согласия на установку:
  счётчики шапки (`economy.today/turn/session`) считаются в фоновом потоке `lib/economy-worker.cjs` (раньше на
  сессии 26 МБ блокировали окно ~1.5 с каждые 30 с); сбор сведений о проекте (обход дерева + git) асинхронный
  (`readProjectSources`); при старте удаляются `tmp/nodo-*` прошлых запусков старше часа (`lib/temp-sweep.cjs`,
  в DEV удалено 144). DEV: DeepSeek ответил, шапка считает. Для DEV достаточно прокси Happ
  `HTTPS_PROXY=http://127.0.0.1:10808`. В рабочей папке нужна ссылка `node_modules -> runtime/node_modules` для тестов.
- **25.09 13:40 рабочий NODO обновлён до 1.4.42** (согласие владельца): ready + 30 с удержания, через минуту тот же
  харнесс; подчистка удалила 86 старых `tmp/nodo-*` (66 МБ -> 5.7 МБ; 3 папки моложе часа остались).
- **1.4.43 собрана и проверена в DEV** (коммит `286c6a9`, `~/Projects/NODO-releases/1.4.43/`), ждёт согласия:
  задачи DeepSeek опрашивают только новые события (`inspect` с `afterSeq` в плагине; ответ и конец хода копятся
  на задаче) - раньше вся сессия пересылалась каждые 1.2 с; чекпоинты - последние 200 (у владельца их 3, 12 КБ).
  DEV: многошаговая задача Done, ответ целый, pollSeq 3 -> 30.
- **25.09 13:49 рабочий NODO обновлён до 1.4.43** (согласие владельца): ready + 30 с, через 45 с тот же харнесс.
- **1.4.44 собрана и проверена в DEV** (коммит `20de783`, `~/Projects/NODO-releases/1.4.44/`), ждёт согласия:
  супервизор не считает прогрессом повтор той же меняющей команды без других изменений (лазейка `touch && curl`);
  запоздалый ответ фонового верификатора игнорируется, если ход закончился, сменился или сдвинулся. Тесты
  падают без правки и проходят с ней. DEV: задача «создай и прочитай файл» Done - обычная работа не блокируется.
- Первые 46 мин после выключения роутера (25.09 13:06-13:52): кэш 98.6% (24.09 весь день - 92.6%), 2 смены
  уровня. Скрипт: `~/Projects/nodo-devtools/econ-check.py`. Полный день проверить 26.09 по слову владельца
  (запланированные задачи владелец не хочет).
- **25.09 14:20 рабочий NODO обновлён до 1.4.44** со второй попытки. Первая (14:10) откатилась сама: запуск ждал
  окно Связки ключей («NODO Safe Storage»), владельца не было у экрана 2 мин. Причина: сборки подписаны ad-hoc,
  у каждой новая подпись, и macOS заново спрашивает доступ. Владелец жмёт «Разрешить всегда» на каждой установке.
  Устанавливать только когда владелец у экрана; переключателю давать `--timeout 300000`.
  Лечение (ждёт решения владельца): подписывать сборки постоянным локальным сертификатом, тогда «Разрешить
  всегда» будет действовать и для следующих версий.
- **Постоянная подпись сборок** (согласие владельца 25.09): в связке ключей `login` создан самоподписанный
  сертификат «NODO Local Signing» (отпечаток A4CDC6B7…13A2, 20 лет, доверие системе не добавлялось).
  `scripts/build.cjs` подписывает им сборку, если он есть (иначе ad-hoc); требование подписи теперь
  `identifier "local.nodo.harness.rc" and certificate root = H"a4cdc6b7…"` - одинаковое у всех версий, поэтому
  «Разрешить всегда» для «NODO Safe Storage» должно пережить обновления. 1.4.45 собрана (`~/Projects/NODO-releases/1.4.45/`),
  DEV с этой подписью стартует, DeepSeek Done. На установке 1.4.45 окно появится последний раз; проверить на 1.4.46.
- **25.09 14:34 рабочий NODO обновлён до 1.4.45** (подписан «NODO Local Signing»), ready + стабилен. Проверить на 1.4.46, что окно Связки ключей больше не появляется.
- «Утечка слушателей rc:changed» оказалась ложной: все 8 хуков снимают подписку, просто подписчиков 11 при
  пороге Node 10. Порог поднят до 50 в `preload.cjs`.
- **Набор тестов полностью зелёный** (25.09): 305 прошли, 0 упали, 6 пропущены (miniflare / локальный релиз).
  Четыре «вечных» падения были устаревшими тестами, код не виноват: брендинг мобильного Remote (без HARNESS,
  серые переходы разрешены), поле attachments в remote-api, установщик (WatchdogScope.swift, namespace
  com.efrecord, resume-queue/update-policy в фикстуре, node-libs необязателен, runner factoscope в ~/.dsh),
  мобильный e2e (Project и сессии в выдвижном меню).
- 1.4.46 собрана (`~/Projects/NODO-releases/1.4.46/`), DEV Done. Ждёт установки - заодно проверка, что окно
  Связки ключей больше не появляется.
- **25.09 14:58 рабочий NODO обновлён до 1.4.46**: ready + стабилен, запуск до движка 5.6 с. Владелец подтвердил: окно Связки ключей на 1.4.46 не появилось - постоянная подпись
  «NODO Local Signing» работает, «Разрешить всегда» переживает обновления.
- **Замер правок 19.09 (полный контекст, без сжатия, вывод не режется)**, 25.09, только чтение журналов рабочего профиля.
  Главное: в рабочем профиле они уже действуют с 19.09 23:22 через данные профиля, а не через код: `economy-flags.json`
  (`compactionRatio 0.95`, потолки вывода 2 МБ), `context-modes.json` (`full`), `dsh/profiles/web/cordis.patch.yml`
  (`thresholdRatio 0.95`, `maxInlineBytes 2000000`). Установка релиза этот файл не перезаписывает (копируется один раз).
  До (17-19.09) / после (20-25.09): сжатий 72 / 0; контекст запроса медиана 80k / 119k, p90 159k / 384k, макс 263k / 754k;
  запросов больше 200k: 50 / 1077; кэш 92.5% / 95.7%. Вывод инструментов больше 16 КБ после 20.09: 158 из 4270.
  Цена (симуляция на 20-25.09, цены владельца с пиком): старое сжатие сэкономило бы $1.8-2.3 из $13.75 - то есть
  без сжатия расход выше на **~$0.3-0.4 в день (+15-20%)**; неурезанный вывод - ещё ~$0.04 в день.
  Остаток в коде `main` (все правила без выбрасывания, файлы-ссылки целиком, без обрезки источников) при режиме `full`
  почти ничего не добавляет: маркер обрезки источников в сообщениях за 17-25.09 не встретился ни разу.
  Решение (оставить / вернуть сжатие) - за владельцем. Скрипты: scratchpad `ctx.py`, `sim.py`, `tools.py` (не в репозитории).
- **Мягкий порог сжатия** (решение владельца 25.09): сжатие харнесса после 400k токенов (`thresholdRatio 0.40`), дословно
  остаются последние 150k (`retainTokens 150000`). Поставлено в рабочий профиль (`dsh/profiles/web/cordis.patch.yml`, копия
  `.bak-*-softcompact`; `economy-flags.json compactionRatio 0.40` - только для журнала триггера) и в шаблон `profile.patch.yml`
  для новых профилей. Действует со следующего запуска рабочего NODO. DEV с этим порогом стартует без ошибок плагинов.
  По данным 20-25.09 такой порог сработал бы ~5 раз за 6 дней (≈ -$0.05 в день): это страховка от переполнения, а не экономия.
- **Reference-блок NODO раз за ход** (25.09, `lib/project-runtime.cjs`). Замер 20-25.09: блок (~6 КБ) вставлялся почти
  на каждом шаге - 6.8 из 7.3 МБ были повторами внутри хода (510 байт-в-байт, остальные отличались строкой подсказки
  или списком документов/git, который правил сам агент). Теперь: полный блок - на новый ход; внутри хода - только если
  изменилось что-то кроме doc/git; если изменилась только подсказка - уходит одна подсказка. Оценка: -6.7 МБ контекста
  и ≈ -$0.18 в день (≈8% расхода). Тест `project-runtime.test.cjs` падает без правки. DEV: задача в 5 шагов - блок
  один раз, ответ верный; следующий ход - блок пришёл снова. Тесты 306/0/6. В выпуск - со следующей сборкой.
- **1.4.47 собрана** (`~/Projects/NODO-releases/1.4.47/`, подпись «NODO Local Signing»): reference-блок раз за ход.
  DEV 1.4.47: задача в 3 шага Done, блок один раз, ответ верный. Тесты 306/0/6. Ждёт согласия на установку.
- **25.09 15:27 рабочий NODO обновлён до 1.4.47** (согласие владельца): ready + 30 с удержания, через минуту тот же pid,
  новых ошибок в `dsh.log` нет. С этим запуском действует мягкий порог сжатия (400k / 150k). Откат: `nodo-switch.cjs rollback`.
- **1.4.48 - новый дизайн** (по эскизу, согласие владельца 25.09; холст https://claude.ai/artifact/WNTgHxN7yKzPtTd1gxNaS6).
  Тема поверх интерфейса DSH (`extension/client.js`, токены `--dsw-*` тёмной темы): графит #121314/#161719/#1A1B1D/#222427,
  текст #E8E6E1, один акцент - бирюза #4FB7A8 (заменил и синий DeepSeek `--dsw-static-deepseek-*`), ошибки #E5735A.
  Строки «Context injection» скрыты из чата (остаются в Trajectory); строки инструментов серые, красные только провалы;
  наложение «Workspace Write»/агент в поле ввода исправлено; в шапке одна цифра «$X сегодня ±N%», остальное в карточке
  по клику (карточка по-русски). Логотип: N из четырёх узлов, активный узел бирюзовый (`assets/logo-master.svg`,
  `logo.json`, лок-ап «nodo HARNESS» без рамки), иконка Dock пересобрана.
  **Фишка «живой узел»** (`lib/live-node.cjs`): узел логотипа в Dock и в боковой панели показывает состояние агента -
  пульсирует, пока идёт работа, янтарный - ждёт владельца, коралловый - последняя задача упала (гаснет через 10 мин).
  Тесты бренда переписаны под новую палитру (`brand-palette.test.cjs`), `live-node.test.cjs` новый. Тесты 309/0/6.
  DEV 1.4.48: тема, логотип, пульс узла во время задачи проверены, DeepSeek Done. Интерфейс DSH пока на английском
  (Chat, Workspaces, New Session) - перевод следующим шагом. Сборка `~/Projects/NODO-releases/1.4.48/`, ждёт установки.
- **25.09 16:01 рабочий NODO обновлён до 1.4.48** (согласие владельца): ready + 30 с удержания, через минуту тот же pid,
  новых ошибок нет. Новый дизайн и «живой узел» в работе.
- **Голос Ефрекорд** (25.09, по согласию владельца; вне репозитория - в `~/.dsh` и Wiki): исследование 120 клиентских диалогов
  2020-2026 + NODO 10-25.09 (7 параллельных разборов) -> свод `20 Wiki/projects/efrecord-voice-guide.md` и библиотека
  `efrecord-voice-examples.md` (143 удачных, 78 неудачных). Мост (`~/.dsh/plugins/telegram-bridge/playbook_hint.py`) добавляет
  к каждому авто-черновику формулу, запреты, строку ситуации и 2-3 эталона. `~/.dsh/AGENTS.md`: «телеграфный стиль» заменён
  «Голосом Ефрекорд»; имя клиента - редко и в его форме (заменяет запрет 14.09); формат показа черновика - только текст
  черновика, без «Совет:». Бэкапы `*.bak-20260925-voice`.
- **1.4.49 собрана** (`~/Projects/NODO-releases/1.4.49/`): чистый клиентский чат (`installClientChatView` в `extension/client.js`):
  сообщение клиента слева с именем, «ушло клиенту», один черновик «Предлагаю ответить»; задание моста, вызовы инструментов,
  размышления, строки контекста и промежуточные реплики скрыты (Trajectory - всё как было). Мост просит у агента только текст
  черновика (`telegram-bridge-live.mjs`), строка «Совет:» снята. Тест `client-chat-view.test.cjs`. Тесты 311/0/6.
  DEV: вид проверен на имитации клиента, DeepSeek Done. В DEV не гонять задачи агента с инструментами: владелец не может
  выдать DEV разрешение на всё, окна подтверждения всплывают у него. Ждёт установки.
- **25.09 1.4.49 установлена** (согласие владельца): ready + 30 с, через минуту тот же pid, новых ошибок нет. Чистый клиентский чат в работе.
- **1.4.50 - дизайн «Нить»** (эскиз-страница «Нить - весь стиль» на холсте https://claude.ai/artifact/WNTgHxN7yKzPtTd1gxNaS6,
  «внедряй» владельца 25.09). Интерфейс по-русски через штатные словари DSH: плагин берёт сервис `locale`, добавляет язык
  `ru` (41 пространство имён, 1239 строк, `NODO_RU` в `extension/client.js`), включает его один раз (`nodo.locale.ru`),
  English остаётся в Настройках. «Deep diving...» заменён знаком N, который рисует себя, и словом фазы: «Связываю»,
  «Читаю», «Правлю», «Выполняю», «Пишу ответ» + деталь с бегущим бликом (`installNit`, только CSS и data-атрибуты на
  `EvIC1a_turnStatus`). Чат - нить: вертикальная линия, узлы (полый - ты, бирюзовый - ответ, бусины - свёрнутые шаги
  «Ход мысли», коралл - провал), сообщение владельца слева с подписью «Ты»; строки «Системный промпт» и «Внедрение
  контекста» скрыты (классификатор строк понимает русские названия). Графит с сеткой узлов, боковая панель и центр -
  отдельные скруглённые листы, поле ввода - стекло, кромка окна бежит бирюзой, пока агент работает (янтарь - ждёт).
  Пустой экран: «НОВАЯ СЕССИЯ / Что делаем в «проект»?» вместо второго логотипа. Project/Browser/Diagnostics из поля
  ввода собраны в одно «⋯», меню шапки по-русски. Клиентский чат не тронут (нить только в обычных чатах).
  Тесты 315/0/6 (`nit-design.test.cjs` новый, `header-layout` под новое решение). DEV: вид, русский, знак N во время
  задачи без инструментов, меню «⋯» проверены. Шрифты (согласие владельца): IBM Plex Sans (переменный 400-600) и Mono
  400/500, латиница+кириллица, SIL OFL, `assets/fonts/`, встроены data:-ссылками в `client.js` скриптом
  `scripts/embed-fonts.cjs` (+128 КБ); переопределены `--dsw-font-family`/`--ds-font-family-code`. Space Grotesk не взят:
  в нём нет кириллицы. Тесты 316/0/6. Сборка `~/Projects/NODO-releases/1.4.50/`.
- **25.09 18:20 рабочий NODO обновлён до 1.4.50** (согласие владельца): ready + 30 с удержания, через минуту тот же pid. Дизайн «Нить» и шрифты IBM Plex в работе.
- **Подвисания окна (25.09, 1.4.51).** macOS зафиксировал у 1.4.50: 2,1 ГБ записи на диск за 10 минут главным процессом
  (3,5 МБ/с, стек - таймеры -> `fs.writeSync`). Причина: синхронная запись с fsync в главном потоке огромных файлов -
  `usage.json` (9 МБ, переписывался целиком на каждый ответ модели), `workstation.json` (5,5 МБ, на каждое `changed()`),
  `tasks.json` (0,8 МБ, дважды в секунду при стриминге). Исправлено: журнал расхода копится в памяти и пишется раз в 3 с
  (+ `flush()` при выходе); состояние пишется не чаще раза в 2 с, а обновление экрана осталось через 120 мс; `tasks.json`
  при стриминге - раз в 2 с; файлы больше 256 КБ пишутся без отступов (`lib/io.cjs`). Режим клиентского чата больше не
  перечитывает текст всех пузырей на каждый кадр. Тест `write-throttle.test.cjs`. Тесты 319/0/6. DEV: DeepSeek Done.
  Сборка `~/Projects/NODO-releases/1.4.51/`, ждёт установки.
- **1.4.52 собрана** (`~/Projects/NODO-releases/1.4.52/`): «Нить», второй проход + фикс подвисаний 1.4.51 из соседней сессии.
  Боковая панель: проект - узел вместо папки (янтарь - ждёт тебя, бирюзовый пульс - работает, коралл - сбой),
  сессии висят ветками на нити, клиентские сессии - кружок с буквой вместо эмодзи 👤 (эмодзи скрыт `::first-letter`,
  текст и ключи закреплений не меняются), оранжевая булавка стала бирюзовой засечкой, штатные точки DSH скрыты.
  Вкладки «Чат / Ход работы» - сегментный переключатель; «Ход мысли · 5 действий · 2 сообщения»; пустой экран -
  карточки «Продолжить» из ветки проекта под полем ввода; черновик клиенту - пунктирная бирюзовая карточка; пометки
  по смыслу перекрашены в палитру (без зелёного и синего), в сообщениях владельца не ставятся. DSH рисует углы
  суперэллипсом (`corner-shape`) - узлам возвращён круг. Проход `installNit` не чаще 4 раз в секунду.
  Тесты 320/0/6. DEV: панель, монограммы (имитация), карточки, вкладки проверены.
- **25.09 1.4.52 установлена** (согласие владельца; сборка сессии дизайна, коммит a663f9f = дизайн «Нить» + фикс подвисаний 1a654e5): ready + 30 с, через минуту тот же pid, новых ошибок нет; usage.json уже компактный (9 -> 7 МБ). 1.4.51 не ставилась.
- **1.4.53 собрана** (`~/Projects/NODO-releases/1.4.53/`): «Нить», вариант Б - строже (владелец: «раскрасил по-детски, всё мигает»).
  Ничего не мигает, кроме линии по верху окна во время работы и знака N в чате (живой узел логотипа и янтарная кромка -
  статичны). Боковая панель: без узлов и веток, статус - слово справа вместо времени («ждёт» янтарём, «пишет» серым,
  «сбой» кораллом), клиенты - кружок-контур с буквой (любое эмодзи в начале названия, не только 👤). Черновик клиенту -
  между двумя тонкими линиями с серой подписью «Черновик»; отчёт «Отправлено (id …)» - обычный текст, а черновик перед
  ним подписан «Ушло клиенту». «Ход мысли» - простой серый текст, «Продолжить» на пустом экране - список, не карточки.
  Тесты 321/0/6. DEV проверен (клиентский чат, имитация статусов и монограмм). Ждёт установки.
- **25.09 19:13 рабочий NODO обновлён до 1.4.53** (согласие владельца, после окончания задач): ready + 30 с удержания. Вариант Б «Нити» в работе.
- **1.4.54 собрана** (`~/Projects/NODO-releases/1.4.54/`): клиентский чат без карточек, вариант A2 (владелец: «карточки - нейрослоп»).
  Пузырей и рамок нет: над сообщением имя, у каждого клиента свой приглушённый цвет (6 оттенков, `nitHue` по первому
  слову имени) - тот же цвет у его буквы в боковой панели; реплики владельца - серые с подписью «Ты»; черновик между
  двумя бирюзовыми линиями с бирюзовой подписью, отправленный - серый «Ушло клиенту». Тесты 322/0/6. DEV проверен.
- **25.09 20:25 рабочий NODO обновлён до 1.4.54** (согласие владельца): ready + 30 с удержания. Клиентский чат A2 в работе.
- **Обвес агента (25.09, 1.4.55).** Разбор 10 дней: 360 вызовов встроенных инструментов харнесса падали с «tool is not
  enabled» (`send_message` 161, `present` 118, `list_agents` 55, `ask_user_question` 26). В `lib/tool-policy.cjs` открыты
  `send_message`, `interrupt_agent`, `list_agents`, `present`; `ask_user_question` закрыт намеренно (остановил бы авто-черновик).
  Навыки `kp-efrecord` и `efrecord-dogovory` были только в `~/.dsh/skills`, агент их звал и не находил - скопированы в
  профиль `NODO/dsh/skills` (действует сразу). Новые MCP не нужны: Obsidian агент ведёт файлами напрямую, браузер уже есть
  (Chrome DevTools MCP + rc_browser), Drafts/Figma/Word на этом Mac не установлены. Тесты 322/0/6, DEV Done.
  Сборка `~/Projects/NODO-releases/1.4.55/`, ждёт установки.
- **25.09 1.4.55 установлена** (согласие владельца): ready + 30 с, через минуту тот же pid, ошибок нет.
- **1.4.56 собрана** (`~/Projects/NODO-releases/1.4.56/`): починено окно «Настройки» - в 1.4.50-1.4.55 оно сжималось до ширины
  боковой панели: `backdrop-filter` на `.hHd-Xa_root` делал панель контейнером для `position:fixed`, а окно настроек DSH
  рисуется внутри неё. Стекло с панели и поля ввода снято (сплошной графит), тест на это добавлен. Разделы настроек NODO
  по-русски (Удалённый доступ, Обновления, Интерпретатор, Супервизор, Интеллект). Тесты 323/0/6. DEV: окно 800x800.
- **25.09 22:58 рабочий NODO обновлён до 1.4.56** (согласие владельца): ready + 30 с удержания. Окно «Настройки» снова полноразмерное.
  Проверка DEV перед установкой: меню модели, доступа, команд, карточка расхода, «Ход работы», панель «Проект» открываются
  нормально. Не переведены: панель «Проект» (NODO), значки ролей в «Ходе работы» (оранжевый/зелёный DSH).
- **1.4.57 собрана** (`~/Projects/NODO-releases/1.4.57/`): быстрые ответы под черновиком клиенту, вариант Б. Строка над полем ввода
  («Черновик · Отправить ⌘↵ · Короче · Теплее · или напиши правку ниже»), пока последнее в клиентском чате - неотправленный
  черновик, агент не пишет и поле пустое. Кнопка вставляет в поле то же слово, что владелец набирал руками («Отправляй»;
  «Сделай короче/теплее. Не отправляй, покажи новый черновик.») и жмёт штатную «Отправить»: дальше агент сам зовёт
  `telegram_reply`, проверка моста «последняя реплика владельца, не [мост]» та же. ⌘↵ при пустом поле = «Отправить».
  `installQuickReplies` (узел в body, не в React). Тесты 324/0/6. DEV: вид, скрытие при наборе, «Короче» и ⌘↵ проверены
  с перехватом кнопки отправки (клиенту ничего не ушло).
- **1.4.58 собрана** (`~/Projects/NODO-releases/1.4.58/`, включает 1.4.57 - она не ставилась).
  Панель «Клиент» справа (вариант «досье»): имя цветом клиента, «с 17.09 · 3 779 сообщений · последнее 23:03», ЭТАП,
  ОТКРЫТО (бирюзовая черта), ЧТО ЗНАЕМ, УШЛО (число подтверждённых отправок и время последней из `send-ledger.jsonl`).
  Данные - только из карточки `NODO/clients/<чат>.md` (`lib/client-card-view.cjs`, запрос `client.card`, только чтение):
  этап и вопросы - строки агента «Этап:», «Открыто:», «Ждёт:», «Дальше:». В правила агента
  (`NODO/dsh/AGENTS.md`, копия `.bak-20260925-stage`) добавлено: первой строкой «Смысл» - `Этап: …`, каждый вопрос -
  `- Открыто: …`. Панель сама открывается один раз за запуск на каждую клиентскую сессию, ещё - «⋯ -> Клиент».
  Пометки по смыслу (`installStatusMarks`) больше не лезут в `.nit-card`/`.nit-quick`.
  Превью медиа (владелец: «хочу видеть всё, а не бегать по ссылкам»): карточка у каждого упоминания файла, не только
  когда абзац - один путь; файлы из сообщений клиента («[файл: …]») показываются под сообщением, путь в тексте сокращён
  до имени (меняется только текст узла); превью одного сообщения - в ряд; Word/Excel/PowerPoint/Keynote/Pages/HEIC/TIFF/
  PSD/SVG - картинкой Quick Look, как PDF; пути с `~/` работают. Строка быстрых ответов получила фон.
  Тесты 330/0/6. DEV: панель на копии реальной карточки, фото/PDF/docx/голосовое в клиентском сообщении проверены.
- **25.09 1.4.58 установлена** (согласие владельца): ready + 30 с удержания, процесс жив. Панель «Клиент», превью медиа и быстрые ответы в работе.
- **1.4.59 собрана** (`~/Projects/NODO-releases/1.4.59/`): досье для всех проектов и ⌘K (владелец: «фишку по клиентам - для всех
  проектов, иначе продукт узконаправленный»). Панель «Досье» (бывшая «Клиент»): у любой сессии - досье её проекта (этап, ЖДЁТ ТЕБЯ
  янтарной чертой, открыто, решено, сделано, что знаем; сессии, первое и последнее касание считает NODO по папке сессий DSH),
  в клиентском чате - досье клиента со строкой «проект · этап проекта» сверху. Файл досье проекта -
  `NODO/dossiers/<папка>-<8 знаков sha256 пути>.md` (`lib/dossier.cjs`, запросы `dossier.get`/`dossier.list`, только чтение);
  путь - одной строкой в кэшируемой секции `nodo-project-stable` (`lib/project-runtime.cjs`), правило - раздел «Досье проекта»
  в `NODO/dsh/AGENTS.md` (бэкап `.bak-20260925-dossier`). Панель открывается сама один раз за запуск на каждую сессию;
  старая панель «Проект» стала «Настройка проекта» (ссылка внизу досье). ⌘K (вариант А): окно по центру, группы Клиенты/Сессии/
  Проекты (группа с лучшим совпадением выше), поиск по началу, подряд и буквами вразбивку, «ждёт» янтарём, у проекта - этап из
  досье; открытие штатным `sessions.open`, проект - его последняя сессия. Работает и на русской раскладке (⌘Л).
  Тесты 331/0/6 (установщик падал по таймауту под нагрузкой, отдельно проходит). DEV: досье проекта и клиента, ⌘K, поиск и переход.
- **26.09 1.4.59 установлена** (согласие владельца): ready + 30 с удержания. Досье всех проектов и ⌘K в работе.
- **1.4.60 собрана** (`~/Projects/NODO-releases/1.4.60/`): превью сессии при наведении в стиле «Нить» (владелец: «превьюшки конченные»): вместо эмодзи 👤 - буква в кружке цветом клиента, статус словом (янтарь - ждёт, коралл - сбой), без зелёной точки; плашка графит с тонкой рамкой. Тесты зелёные.
- **26.09 1.4.60 установлена** (согласие владельца): ready + 30 с удержания.
- **1.4.61 собрана** (`~/Projects/NODO-releases/1.4.61/`): календарь Apple и нить в клиентском чате.
  Календарь (вариант «В»): вкладка «Календарь» справа - 14 дней из календаря Apple через EventKit из JXA (`lib/calendar.cjs`,
  без запуска Calendar.app, ~0,1 с), только чтение; доступ macOS спрашивает один раз по кнопке «Открыть доступ»
  (ключи `NSCalendars*UsageDescription` в `scripts/build.cjs`). День, названный в последнем сообщении («в субботу часа в два»,
  «завтра в 11:30», «27.09 к 14», «3 октября в 10 утра»), сам открывается во вкладке с пометкой «свободно»/«занято: …» на это
  время. «❌» в названии - серым зачёркнуто. Агенту - снимок `NODO/calendar/days.md` раз в 5 минут и правило «Календарь
  владельца» в `NODO/dsh/AGENTS.md` (бэкап `.bak-20260926-calendar`); события ставит по-прежнему секретарь.
  Попутно: меню «⋯» в шапке не открывало ни одной панели («open is not a function» - состояние перекрывало функцию),
  чистка счётчиков оболочки прятала любое «11:00» и в наших панелях - оба исправлены.
  Нить в клиентском чате (вариант Б без горизонтальных линий): линия и узлы как в обычных сессиях - клиент залитым узлом
  своего цвета и полужирным текстом, ты - полым узлом и курсивом, черновик модели - бирюзовым узлом и обычным текстом,
  ушедшее клиенту - серым. Линии над и под черновиком убраны. Тесты 336/0/6. DEV: календарь на реальных данных
  (доступ владелец дал DEV), нить клиента.
- **26.09 1.4.61 установлена** (согласие владельца): ready + 30 с удержания. Доступ к календарю для рабочего NODO владелец даёт кнопкой во вкладке.
- **1.4.62 собрана** (`~/Projects/NODO-releases/1.4.62/`): число «ждёт тебя» на иконке Dock (вариант А) - янтарём прямо в верхнем
  правом узле логотипа, 10 и больше - «9+». Считается: клиентские сессии со «ждёт» (как в боковой панели) плюс строки
  «Ждёт тебя» из досье проектов. Картинку рисует окно (`installDockCount`), ставит main (`dock.base`/`dock.badge`); пока число
  больше нуля, оно заменяет узел состояния, при нуле возвращается обычный узел. Календарь: «❌» в названии снова показывается как
  есть (владелец: это пометка важного, не отмена). Тесты 337/0/6. DEV: картинка с числом собрана и поставлена без ошибок
  (Dock на этом Mac скрывается, глазами не проверен).
- **26.09 1.4.62 установлена** (согласие владельца): ready + 30 с удержания.
- **1.4.63 собрана** (`~/Projects/NODO-releases/1.4.63/`): цепочка оплаты ведётся NODO, а не памятью агента (владелец: «рвёт цепочку,
  не видит счёт Коли, хочу стабильно»; шаги уходят сами - решение владельца). `lib/order-chain.cjs`: заказ - файл
  `NODO/orders/<чат>-<время>.json` с шагом (new -> await_data -> await_invoice -> await_receipt -> done), каждое действие с
  отметкой «сделано» (повтор после сбоя не шлёт дважды). Мост (`services/telegram-bridge-live.mjs`) отдаёт цепочке каждое
  входящее: данные клиента (ФИО, почта, телефон, доставка - разбор без модели) -> сообщение Коле с полными данными и ценами;
  файл от Коли, где PDFKit/Vision видит «Счёт на оплату №», -> сверка суммы -> клиенту счёт и просьба о квитанции; файл клиента,
  похожий на платёж, -> квитанция Коле, карточка без цен в «Заказы» (`telegram_orders.py publish --confirmed`), спасибо
  клиенту, Лёхе номер заказа и «свяжись по сроку». Посторонние сообщения клиента и Коли (текст, КП, фото комнаты) цепочку не
  двигают и идут обычным черновиком; забранные цепочкой сообщения черновик не получают. Стоп («сумма не совпала», «счёт
  подходит к двум заказам», сбой отправки) - «Ждёт тебя» в досье клиента и число на Dock; снимает владелец
  `~/.dsh/bin/nodo-order resume <id>`. Заводит заказ агент (`nodo-order start`, правило в `NODO/dsh/AGENTS.md`, бэкап
  `.bak-20260926-orderchain`; Wiki `efrecord-client-rules.md`) или владелец кнопкой «Оформить» под черновиком. Выключатель:
  `"orderChain":{"enabled":false}` в `bridge-config.json`. Тексты утверждены владельцем 26.09. Тесты 343/0/6
  (`order-chain.test.cjs`: вся цепочка, болтовня Коли, КП, фото комнаты, неверная сумма, два заказа, сбой). Классификация
  проверена на настоящих файлах из media (счёт №101, квитанция-фото, КП, платёжка). В DEV мост Telegram выключен: живая
  проверка - первый настоящий заказ.
- **26.09 1.4.63 установлена** (согласие владельца): ready + 30 с удержания. Замечание: за 2 минуты до установки в двух сессиях были записи (вероятно, хвост авто-черновика), установка не дождалась тишины.
- **1.4.64 собрана** (`~/Projects/NODO-releases/1.4.64/`): светлая тема «Нить», вариант В - почти белая бумага #FAF8F3 на всё окно,
  боковая панель отделена тонкой линией, чернила #1F1E1C, бирюза #2E8C7F, янтарь #A87A1E, коралл #C0543A, цвета клиентов
  темнее. Переключается штатным «Настройки -> Внешний вид» (светлая = body без `data-ds-dark-theme`). Устройство: каждый цвет в
  стилях NODO на лету становится `var(--nit-XXXXXX,#XXXXXX)` (`nitThemed`), тёмная тема - запасные значения, то есть прежняя
  байт-в-байт; светлая переопределяет переменные одним блоком (`nitLightCss`) и тонирует фон DSH, поле ввода и кнопку отправки;
  логотип в светлой - та же SVG с тёмными чернилами (`nitLightLogo`). Без backdrop-filter/transform (окно «Настройки» 800x800
  проверено). Попутно: вкладка «Календарь» больше не открывается от голого «сегодня/завтра» - только время, день недели или дата.
  Тесты 344/0/6. DEV: светлая (чат, клиентский чат, боковая панель, календарь, ⌘K, Настройки) и тёмная проверены.
- **26.09 1.4.64 установлена** (согласие владельца, без активных задач): ready + 30 с удержания.
- **1.4.65 собрана** (`~/Projects/NODO-releases/1.4.65/`): П1 «Деньги на столе» (вариант Б) и Д1 «Живая N» (вариант А «огни»),
  решения владельца 26.09. Шапка: янтарная строка «N ₽ на столе», по клику - клиенты с суммой, что это и сколько дней тишины,
  «Напомнить» (открывает чат клиента и пишет агенту «Напомни мягко… Не отправляй, покажи черновик» - отправка по обычным правилам).
  Суммы только из заказов цепочки оплаты (`NODO/orders`, оплаченный заказ снимает клиента) и строки `- Сумма: N ₽ - что` в «Смысле»
  карточки (`lib/money-table.cjs`; метка «Сумма» в `lib/dossier.cjs`, строка с меткой всегда отдельный пункт). Правило агенту - в
  `NODO/dsh/AGENTS.md` (бэкап `.bak-20260926-money`). Разово заполнено 6 карточек, где цена названа и не оплачена (бэкап
  `NODO/clients.bak-20260926-money`): итого 260 140 ₽ (Безбородько 167 240, Александр 29 700, vishe_oblakow 18 800, An257mn 16 200,
  S447116 16 200, PlatformBreez 12 000) - список показан владельцу на проверку. Живая N: узлы логотипа - клиенты (слева сверху,
  янтарь: готов ответ), агенты (справа сверху: бирюза - работают, янтарь - «ждёт тебя», коралл - упала), календарь (слева снизу:
  событие в ближайший час), деньги (справа снизу: янтарь - заказ на стопе, бирюза - счёт ждёт оплаты). Горит - залит, пусто - кольцо,
  без чисел и мигания, смысл в подсказке; клик ведёт туда (сессия, «Досье», «Календарь», список «на столе»). Та же N в Dock
  (картинку рисует окно, стоит всегда - пульс Dock и мигающая точка над логотипом убраны; линия сверху окна осталась). Тесты 349/0/6
  (`money-table.test.cjs`, `nit-design`). DEV: строка, список, «Напомнить» (сухой режим, без отправки), клик по узлу, логотип, картинка Dock проверены.
- **26.09 1.4.65 установлена** (согласие владельца, тихо): ready + 30 с удержания. Правило агенту «сайт - истина, ссылка на товар» (бэкап `.bak-20260926-site`; ткани - фото из папки, сроки/доставка - шаблон). П2: честная точность по истории - из 66 настоящих черновиков 26 ушли как есть (39 %), 12 поправлены, 24 отклонены, 4 неясно; 53 «без правок» - собственные сообщения владельца без черновика, в счёт не идут (отчёт `docs/project/P2-draft-accuracy-2026-09-26.md`).
- **1.4.66 собрана**: «Деньги на столе» переделаны по замечанию владельца («реализация тупая, контекст сделок не учтён - показывать
  только где я явно проебался»). Строки агента «Сумма:» в карточках больше не источник (правило агенту убрано, строки сняты):
  раз в 30 минут главный процесс читает переписку каждого клиента за 45 дней (все сессии чата из карточки, реплики с меткой канала,
  `lib/money-review.cjs`), DeepSeek flash через `lib/judgment.cjs` (ключ из Связки, прокси как у баланса) отвечает только «чей ход»
  (we/client/third/done), продажа ли это, названная сумма и причина с датой. В список - только «ход наш» и последнее сообщение старше
  суток; оплачено, ждёт поставку/свою бухгалтерию, коллеги (разметка title-emoji.json, исключения авто-черновика) - нет. Неуверенный
  ответ не показывается. Модель зовётся только по изменившимся чатам (кэш `NODO/money-review.json`, сутки). Заказы на стопе цепочки -
  как раньше. «Напомнить» пишет агенту «Клиент ждёт нас: <причина>… покажи черновик». Карточка vishe_oblakow поправлена (оплачено,
  ждёт поставки). Пробный разбор живых данных 26.09: 39 чатов, 1 неуверенный, в списке 12 (в т.ч. «просил КП - не отправлено»,
  «обещали согласовать день замера - не сделано»). Тесты 352/0/6. DEV: тестовый клиент распознан («обещали фото тканей - без ответа»),
  строка и список проверены. Попутно найдено: запись сессии - много кадров zstd, zlib читает только первый (разбор режет по кадрам).
- **26.09 1.4.66 установлена** (согласие владельца, дождался тишины): ready + 30 с удержания. Выжимка «Как отвечает Ефрекорд» (`20 Wiki/projects/efrecord-answer-digest.md`) утверждена владельцем и подключена: мост (`~/.dsh/plugins/telegram-bridge/playbook_hint.py`, бэкап `.bak-20260926-digest`) кладёт её к каждому сообщению клиента вместо формулы и запретов «Голоса»; раздел разговорника по ситуации и живые примеры - как раньше.
- **1.4.67 собрана**: П2 - честный счёт точности черновиков. Окно пишет каждое действие владельца под черновиком клиенту (Отправить/⌘↵/своё «отправляй» = одобрение, Короче/Теплее, своя правка) в `NODO/draft-outcomes.jsonl` (`lib/draft-log.cjs`); у слова «Черновик» над полем - «<тема>: N из M как есть» (тема по словам клиента: цена, сроки, доставка, оплата, ткань, замер, монтаж, акустика). История до 26.09 не считается: разбор записей сессий (`lib/draft-accuracy.cjs`) показал 1 из 115 «как есть», но в записях черновики перемешаны с отчётами агента, письмами коллегам и собственными сообщениями владельца - цифра ненадёжна. Отправка мимо NODO (прямо в Telegram) журналом не видна. Тесты 358/0/6. DEV: запись и счёт через IPC проверены; подпись над живым черновиком глазами не проверена (в DEV нет клиентских черновиков).
