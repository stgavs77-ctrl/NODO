# Security

Do not report vulnerabilities with real credentials or chat content. Use synthetic profiles. Remote is allowlisted and endpoint-encrypted; it is not a public DSH RPC endpoint. Public source does not grant access to any existing relay. Never disable macOS protections to install a build.
