# Security

Do not report vulnerabilities with real credentials, chat content or personal data; use synthetic profiles.
Remote is allowlisted and endpoint-encrypted and is not a public DSH RPC endpoint. Public source grants no access to any existing relay.
Update packages are Ed25519-signed; the public key in `config/release-trust.json` is a verification anchor, not a secret.
Never disable macOS protections to install a build.
